# Project identifier: VM label and SSH host (ssh <PROJECT_SLUG>-<color>)
PROJECT_SLUG=my-app

# Cloudflare (required for DNS / setup / flip-traffic)
CLOUDFLARE_TOKEN=token-here

# Domains (VM IPs from Terraform)
LIVE_DOMAIN=app.example.com
STAGING_DOMAIN=staging-app.example.com

# Optional: API port for Caddy (default 8000)
# API_PORT=8000

# Optional: clone-app (make clone-app-<color>)
# APP_REPO=https://github.com/owner/repo
# GITHUB_TOKEN=ghp_xxx
# APP_DIR=/root/repo
